name: Deploy to Production

on:
  push:
    branches: [ main ] 

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '23'
        
    - name: Install dependencies
      run: npm ci
      
    - name: Build application
      run: npm run build
      
    - name: Deploy to production server
      uses: appleboy/ssh-action@master
      with:
        host: tools.palbin.com
        username: ${{ secrets.SERVER_USERNAME }}
        password: ${{ secrets.SERVER_PASSWORD }}
        script_stop: true
        script: |
          source ~/.bashrc
          cd /opt/apps/palbin-tools
          /usr/bin/git pull origin main
          /home/siokia/.nvm/versions/node/v23.10.0/bin/npm ci
          /home/siokia/.nvm/versions/node/v23.10.0/bin/npm run build
          /home/siokia/.nvm/versions/node/v23.10.0/bin/pm2 restart palbin-tools 